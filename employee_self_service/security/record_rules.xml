<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        All portal data access is performed via sudo() in controllers with
        manual employee-ownership checks.
        These rules protect the backend views.
    -->
</odoo>
