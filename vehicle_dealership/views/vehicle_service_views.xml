<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_vehicle_service_search" model="ir.ui.view">
        <field name="name">vehicle.service.search</field>
        <field name="model">vehicle.service</field>
        <field name="arch" type="xml">
            <search string="Search Service Records">
                <field name="name"/>
                <field name="vehicle_id"/>
                <field name="technician_id"/>
                <field name="workshop"/>
                <filter string="Scheduled" name="scheduled" domain="[('state', '=', 'scheduled')]"/>
                <filter string="In Progress" name="in_progress" domain="[('state', '=', 'in_progress')]"/>
                <filter string="Completed" name="completed" domain="[('state', '=', 'completed')]"/>
                <separator/>
                <filter string="Routine Maintenance" name="routine" domain="[('service_type', '=', 'routine')]"/>
                <filter string="Repair" name="repair" domain="[('service_type', '=', 'repair')]"/>
                <filter string="Warranty" name="warranty" domain="[('warranty_covered', '=', True)]"/>
                <separator/>
                <filter string="This Week" name="this_week"
                        domain="[('service_date', '&gt;=', (context_today() - relativedelta(weeks=1)).strftime('%Y-%m-%d'))]"/>
                <filter string="This Month" name="this_month"
                        domain="[('service_date', '&gt;=', (context_today() - relativedelta(day=1)).strftime('%Y-%m-%d'))]"/>
                <group expand="0" string="Group By">
                    <filter string="Vehicle" name="group_vehicle" context="{'group_by': 'vehicle_id'}"/>
                    <filter string="Technician" name="group_technician" context="{'group_by': 'technician_id'}"/>
                    <filter string="Service Type" name="group_type" context="{'group_by': 'service_type'}"/>
                    <filter string="Status" name="group_state" context="{'group_by': 'state'}"/>
                    <filter string="Service Date" name="group_date" context="{'group_by': 'service_date'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Vehicle Service Kanban View -->
    <record id="view_vehicle_service_kanban" model="ir.ui.view">
        <field name="name">vehicle.service.kanban</field>
        <field name="model">vehicle.service</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile" default_group_by="state" sample="1">
                <field name="name"/>
                <field name="vehicle_id"/>
                <field name="service_date"/>
                <field name="service_type"/>
                <field name="technician_id"/>
                <field name="total_cost"/>
                <field name="state"/>
                <field name="color"/>
                <templates>
                    <t t-name="card">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="o_kanban_record_top">
                                <div class="o_kanban_record_headings">
                                    <strong class="o_kanban_record_title">
                                        <field name="name"/>
                                    </strong>
                                    <div class="o_kanban_record_subtitle">
                                        <field name="vehicle_id"/>
                                    </div>
                                </div>
                                <div class="o_kanban_record_top_right">
                                    <field name="state" widget="badge"
                                           decoration-info="state == 'scheduled'"
                                           decoration-warning="state == 'in_progress'"
                                           decoration-success="state == 'completed'"/>
                                </div>
                            </div>
                            <div class="o_kanban_record_body">
                                <div>
                                    <i class="fa fa-calendar"/>
                                    <field name="service_date"/>
                                </div>
                                <div>
                                    <i class="fa fa-wrench"/>
                                    <field name="service_type"/>
                                </div>
                                <div t-if="record.technician_id.raw_value">
                                    <i class="fa fa-user"/>
                                    <field name="technician_id"/>
                                </div>
                            </div>
                            <div class="o_kanban_record_bottom">
                                <div class="oe_kanban_bottom_left">
                                    <field name="total_cost" widget="monetary"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Vehicle Service Calendar View -->
    <record id="view_vehicle_service_calendar" model="ir.ui.view">
        <field name="name">vehicle.service.calendar</field>
        <field name="model">vehicle.service</field>
        <field name="arch" type="xml">
            <calendar string="Service Schedule" date_start="service_date" color="state" mode="month">
                <field name="vehicle_id"/>
                <field name="service_type"/>
                <field name="technician_id"/>
            </calendar>
        </field>
    </record>
    <record id="view_vehicle_service_list" model="ir.ui.view">
        <field name="name">vehicle.service.list</field>
        <field name="model">vehicle.service</field>
        <field name="arch" type="xml">
            <list string="Service Records" sample="1">
                <field name="name"/>
                <field name="vehicle_id"/>
                <field name="service_date"/>
                <field name="service_type"/>
                <field name="technician_id" widget="many2one_avatar_user"/>
                <field name="total_cost" widget="monetary" sum="Total"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'scheduled'"
                       decoration-warning="state == 'in_progress'"
                       decoration-success="state == 'completed'"
                       decoration-muted="state == 'cancelled'"/>
                <field name="currency_id" column_invisible="1"/>
            </list>
        </field>
    </record>

    <!-- Vehicle Service Form View -->
    <record id="view_vehicle_service_form" model="ir.ui.view">
        <field name="name">vehicle.service.form</field>
        <field name="model">vehicle.service</field>
        <field name="arch" type="xml">
            <form string="Service Record">
                <header>
                    <button name="action_start" type="object" string="Start Service"
                            invisible="state != 'scheduled'" class="btn-primary"/>
                    <button name="action_complete" type="object" string="Complete"
                            invisible="state != 'in_progress'" class="btn-success"/>
                    <button name="action_cancel" type="object" string="Cancel"
                            invisible="state not in ['scheduled', 'in_progress']" class="btn-secondary"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="scheduled,in_progress,completed"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                    <group>
                        <group string="Service Information">
                            <field name="vehicle_id"/>
                            <field name="service_date"/>
                            <field name="next_service_date"/>
                            <field name="service_type"/>
                            <field name="technician_id" widget="many2one_avatar_user"/>
                            <field name="workshop"/>
                        </group>
                        <group string="Costs">
                            <field name="labor_cost" widget="monetary"/>
                            <field name="parts_cost" widget="monetary"/>
                            <field name="total_cost" widget="monetary" class="oe_subtotal_footer_separator"/>
                            <field name="warranty_covered" widget="boolean_toggle"/>
                            <field name="currency_id" invisible="1"/>
                        </group>
                    </group>
                    <group>
                        <field name="current_mileage"/>
                        <field name="description" placeholder="Describe the service performed..."/>
                        <field name="parts_replaced" placeholder="List parts that were replaced..."/>
                        <field name="notes" placeholder="Additional notes..."/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

</odoo>